<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kartik_chauhan.zoohackathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Authentication.LoginActivity" />
        <activity android:name=".Authentication.PursellerActivity" />
        <activity android:name=".Seller.SellerRegisActivity" />
        <activity android:name=".Seller.addItemActivity" />
        <activity android:name=".Seller.sellerDashBoardActivity" />
        <activity android:name=".Authentication.ForgotActivity"></activity>
    </application>

</manifest>